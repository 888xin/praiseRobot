<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lifeix.robot.dao.PraiseUserDao">

    <insert id="insertPraiseUser" parameterType="com.lifeix.robot.model.PraiseUser">
        insert into praiseuser(accountid,sex)
         values
         (#{accountid},#{sex})
    </insert>

</mapper>